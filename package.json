{
  "name": "vibly-indexer",
  "version": "0.1.0",
  "private": true,
  "description": "SubQuery indexer for vibly-chain OpenGov",
  "scripts": {
    "codegen": "subql codegen",
    "build": "subql codegen && subql build",
    "start:docker": "docker compose pull && docker compose up --remove-orphans",
    "dev": "npm run build && docker compose up --remove-orphans"
  },
  "devDependencies": {
    "@polkadot/api": "^16",
    "@subql/cli": "6.6.3",
    "@subql/common-substrate": "4.5.5",
    "@subql/types": "3.15.0",
    "typescript": "~5.4.0"
  }
}
